import 'package:flutter/material.dart';

class AppColors {
  // Backgrounds
  static const Color splashBackground = Color(0xFF0D0D18);
  static const Color mainBackground = Color(0xFF0D0D14);
  static const Color dotAreaBackground = Color(0xFF1A1A08);
  static const Color cardBackground = Color(0xFF1E1E2A);
  static const Color featureIconBackground = Color(0xFF111120);
  static const Color giftCardBg = Color(0xFF1E1E0A);

  // Brand & accent
  static const Color walletGoldLight = Color(0xFFFFB800);
  static const Color addMoneyGreen = Color(0xFF4CAF50);
  static const Color amber = Color(0xFFFFC107);

  // Text
  static const Color white = Colors.white;
  static const Color textGrey = Color(0xFFAAAAAA);
  static const Color collapsedSubtitleColor = Color(0xFF999999);

  // Utility
  static const Color black = Colors.black;
  static const Color transparent = Colors.transparent;
  static const Color dotColor = Color(0xFF2D2D14);

  // Circle icon button overlay
  static const Color circleButtonBackground = Color(0x59FFFFFF); // white @ 35%

  // Wallet icon gradient (gold body)
  static const Color walletGradientLight = Color(0xFFFFE082);
  static const Color walletGradientMid = Color(0xFFFFB300);
  static const Color walletGradientDark = Color(0xFFE65C00);

  // Wallet icon flap (green top)
  static const Color walletFlapDark = Color(0xFF1B3A0D);
  static const Color walletFlapLight = Color(0xFF3D6B20);
  static const Color walletFlapLock = Color(0xFF0F2008);

  // Wallet icon shadow
  static const Color walletShadowDark = Color(0x50000000); // black @ ~31%

  static const List<Color> confettiColors = [
    Color(0xFFFF3B3B),
    Color(0xFF3BFF3B),
    Color(0xFF3B3BFF),
    Color(0xFFFFFF3B),
    Color(0xFFFF3BFF),
    Color(0xFF3BFFFF),
    Color(0xFFFF8C3B),
    Color(0xFF8C3BFF),
  ];
}
